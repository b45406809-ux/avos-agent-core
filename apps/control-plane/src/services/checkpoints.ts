import type{Env}from"../types";import{randomId}from"../security";import{putVerified}from"./storage";
export async function saveCheckpoint(env:Env,runId:string,body:ArrayBuffer,revision:string){const id=randomId("checkpoint"),stored=await putVerified(env,`runs/${runId}/checkpoints/${id}.json`,body,{runId});await env.DB.batch([env.DB.prepare("INSERT INTO checkpoints(id,run_id,object_key,checksum,repository_revision,created_at) VALUES(?,?,?,?,?,?)").bind(id,runId,stored.key,stored.checksum,revision,Date.now()),env.DB.prepare("UPDATE runs SET checkpoint_id=?,updated_at=? WHERE id=?").bind(id,Date.now(),runId)]);return{id,...stored,repositoryRevision:revision}}
